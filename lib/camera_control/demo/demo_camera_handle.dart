import '../interface/camera_factory.dart';
import '../interface/models/camera_handle.dart';

class DemoCameraHandle extends CameraHandle {
  const DemoCameraHandle() : super(model: CameraModels.demoCamera);
}
